const googleDriveProvider = {
    name: "google-drive",

    async uploadFile(fileData) {
        throw new Error("googleDriveProvider.uploadFile not implemented yet");
    }
};

export default googleDriveProvider;